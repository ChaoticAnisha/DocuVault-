# DocuVault 

## Prerequisites
- Node.js 20+
- PostgreSQL 17 (running, database: docuvault_db)

## Quick Start
1. Clone repo
2. cd docuvault
3. npm install (root)
4. cd backend && npm install && cd ../frontend && npm install && cd ..
